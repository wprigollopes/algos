#include<iostream>

using namespace std;

int main() {
    int N;
    int i=0;
    std::cin>>N;
     int *A = new int[N];
    // While get elements incrementing initial position
    while (cin>>A[i++]);
    // While show elements from N-1 to 
    while (cout<<A[--N]<<' ' && N)
    /* Enter your code here. Read input from STDIN. Print output to STDOUT */   
    return 0;
}